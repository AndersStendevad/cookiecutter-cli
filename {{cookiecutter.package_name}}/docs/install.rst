Installation
============
At the command line::

    pip install .

Or, if you have virtualenvwrapper installed:
.. code-block:: bash

    python -m venv venv
    source venv/bin/activate
    pip install .
